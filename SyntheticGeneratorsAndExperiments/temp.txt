 # STEp 1  - fix the detached state first
cd path/to/submodule
git checkout main


# Generate an alias to update a submodule
git config alias.supdate 'submodule update --remote --merge'

# Update using alias
git supdate
