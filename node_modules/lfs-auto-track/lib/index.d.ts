export declare function parseConditions(conditions: string): {
    exts: string[];
    size: number;
}[];
export declare function getLFSTrackFiles(conditions: string, files: string[], gitattributesFiles: string[], cwd: string, mockFileInfo?: {
    [key: string]: {
        size: number;
    };
}): string[];
export declare function run(conditions: string, files: string[]): Promise<void>;
